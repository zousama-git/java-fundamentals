Day 14 - For Loops

- `for` loop structure: initialization, condition, increment
- Increment operator (++)
- Decrement operator (--)
- Compound assignment operators (+=, -=)
- Thread.sleep() for delays
- For loops are ideal when you know the number of iterations
- The three parts of a for loop: initialization; condition; increment
- Use Thread.sleep(milliseconds) to add delays