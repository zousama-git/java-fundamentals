Day 10 - Enhanced Switches

- A modern replacement for long if-else if chains (Java 14+ feature).
- Uses the -> (arrow operator) instead of colons and the break keyword.
- More concise, eliminates "fall-through" bugs, and allows grouping multiple cases with commas