Day 22 - Linear Search in Arrays
Key Concepts
- Linear search checks each element in an array one by one
- Works for both numbers and strings
- Use loops to iterate through arrays
- break stops the loop once the item is found
- A boolean flag can track whether a value was found
- Index positions start at 0, but can be adjusted for display
- Use .equals() to compare strings (not ==)