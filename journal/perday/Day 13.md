Day 13 
[Weight Converter (WeightConverter.java)]

	A project focusing on if-else if-else logic and basic arithmetic.
	Using comments to outline the program flow (Declare variables → Prompt input → Logic checks).
	Uses == (comparison) instead of = (assignment) within the if-statement conditions. 
	Uses double for precision weights and int for the menu selection.