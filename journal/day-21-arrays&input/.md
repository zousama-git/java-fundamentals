Day 21 - User Input with Arrays

- Arrays can have dynamic sizes based on user input
- Use Scanner to collect user input from the console
- nextInt() reads integers, nextLine() reads full lines
- Loops are used to populate arrays with user input
- Always clear the input buffer after nextInt() using nextLine()
- Loops are used to populate arrays with user input
- Arrays can be traversed using a for loop
- Closing the Scanner prevents resource leaks