Day 23 - Varargs (Variable Arguments)

- Varargs (...) allow methods to accept a variable number of arguments
- Internally, varargs are treated as arrays
- Simplifies method calls when the number of inputs is unknown
- Can be used with different data types (e.g., int, double)
- Must be the last parameter in a method
- Useful for operations like sum, average, etc.